spring:
  application:
    name: 'gateway'
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://52.74.213.195/eureka/
