group 'com.redteamobile'
version '1.0.0'

subprojects {
    apply plugin: 'java'
    apply plugin: "eclipse"
    apply plugin: "idea"

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
        jcenter()
    }
}