spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/SpringCloud_Sell/config-repo
          search-paths: config
server:
  port: 7977
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/




