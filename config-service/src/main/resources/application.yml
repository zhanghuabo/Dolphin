spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhanghuabo/Dolphin
          search-paths: config
          username: zhanghuabo
          password: 11241124zhb
  rabbitmq:
    host: localhost
    port: 5672
    username: root
    password: admin
server:
  port: 7977
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh



