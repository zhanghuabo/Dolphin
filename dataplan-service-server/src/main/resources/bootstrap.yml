spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  application:
    name: dataplan
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/