spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  application:
    name: 'dataplan'
eureka:
  client:
    service-url:
      defaultZone: http://52.74.213.195:8761/eureka/
