spring:
  application:
    name: 'order'
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  zipkin:
    sender:
      type: web
  sleuth:
    web:
      client:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://52.74.213.195:8761/eureka/